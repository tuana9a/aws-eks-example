apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: nginx-classic
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: nginx
